这个文件夹用于存放数据集的图片（PNG）。

目前我已为仓库创建了此占位文件，方便后续上传 PNG 文件。

请把要上传的 PNG 文件直接作为消息附件发送过来，或把它们的文件名和 base64 编码贴在这里，我会把它们上传到 image/ 目录下。

建议的文件名示例：
- image/figure1.png
- image/figure2.png

如果你想要我使用当前会话中显示的两张图（上、下两张示例图），请回复要使用的文件名（例如：figure1.png, figure2.png），我将把它们上传到 image/ 目录。